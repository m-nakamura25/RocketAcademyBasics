//BELOW IS UNFINISHED CODE

/* var mode = "waiting for player 1 order"; //"waiting for player 2", 

var generateRandomNum = function () {
  // produces an integer between 1 and 6
  var randomInteger = (Math.floor(Math.random() * 6) + 1;

  console.log(`random integer: ${randomInteger}`);

  return randomInteger;
};

var main = function (input) {
  var output; 

  if (mode == "waiting for player 1 order") {

    var player1Dice1 = generateRandomNum();
    var player1Dice2 = generateRandomNum();

    output = `Player 1 <br><br>
    You rolled ${player1Dice1} and ${player1Dice2}.<br>
    Choose the order of the dice by entering "1" or "2". `

    mode == "player 1 result"

    return output; 
  } if (mode == "player 1 result") {

    //based on player input "1" or "2", 
  }



  var player2Dice1 = generateRandomNum();
  var player2Dice2 = generateRandomNum();

  var myOutputValue = "hello world";
  return myOutputValue;
}; */

// submit: you rolled x and x. Choose the order.
// based on input 1 or 2, your number is: xx.
// player2 submit: you rolled x and x. Choose the order.
// based on input 1 or 2, your number is: xx.
// xx is the winner.
